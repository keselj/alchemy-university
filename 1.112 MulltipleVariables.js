/*Create two variables a and b with the same value in each. 
Similar to the example above, 
you can first declare a and then set b equal to a*/

const a = 7;
const b = a;

module.exports = { a, b }