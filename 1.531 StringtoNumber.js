// Given a string, convert it to a number.
// If the string is not a number, return 0.

function toNumber(string) {
    if (string = (+string)) {
        return string;
    }
    return 0;
}
